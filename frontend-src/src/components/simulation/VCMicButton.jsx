/**
 * VCMicButton — Microphone push-to-talk button (center bottom)
 * @param {boolean} recording - Whether mic is active
 * @param {function} onToggle - Callback when mic is toggled
 * @param {string} hint - Hint text ('Espace · Prendre la parole')
 * Stub: renders nothing. Full implementation in Step 3.
 */
import React from "react";

export default function VCMicButton({ recording = false, onToggle, hint }) {
  return null;
}
