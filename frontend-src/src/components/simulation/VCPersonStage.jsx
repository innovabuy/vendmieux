/**
 * VCPersonStage — Container for all participant avatars positioned on stage
 * @param {Array} participants - Full participants array from scenario
 * @param {Array} activePeople - Currently present participants
 * Stub: renders empty stage. Full implementation in Step 2.
 */
import React from "react";

export default function VCPersonStage({ participants, activePeople }) {
  return <div className="vc-stage">{/* Positioned person avatars in Step 2 */}</div>;
}
