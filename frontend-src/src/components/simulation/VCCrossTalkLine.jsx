/**
 * VCCrossTalkLine — SVG dashed line between two participants during cross-talk
 * @param {string} fromId - Source participant id
 * @param {string} toId - Target participant id
 * @param {string} color - Line color
 * @param {boolean} visible - Whether line is shown
 * Stub: renders nothing. Full implementation in Step 3.
 */
import React from "react";

export default function VCCrossTalkLine({ fromId, toId, color, visible = false }) {
  if (!visible) return null;
  return null;
}
